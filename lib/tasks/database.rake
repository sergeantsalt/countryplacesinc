# namespace :db do

#   task :backup do
#     system "mysqldump --opt --user=root --password=temp dev_countryplaces userdetails> backup_db" + system ".sql"
#   end


#   task :restore do
#     system "mysqldump --user=root --password=temp dev_countryplaces < backup_db.sql"
#   end

# end